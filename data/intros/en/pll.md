The **21 cases of Permutation of the Last Layer**. The top face is already
entirely yellow; all that is left is to move each piece to its place.

!!! tip "You don't need all 21 at once"
    The **7 cases in the first two sections** (edges only, then corners only)
    let you solve any permutation in two steps.

The **arrows** show where each piece goes. They are computed from the algorithm
itself, so they cannot disagree with it.
